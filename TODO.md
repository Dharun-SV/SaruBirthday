# TODO - WishesApp updates

- [ ] Remove voice/speech synthesis from `src/pages/WishesApp.tsx` (delete speakWish logic + auto-play + speak button behavior)
- [ ] Add background music using `src/Assets/Music/RemoMusic.mp4` and start it after login (welcome page button click). Ensure looping + pause on restart.
- [ ] Add quote highlighting animation for Welcome + Final pages (CSS + small state/timer in TS)
- [ ] Build `npm run build` to ensure TypeScript compiles

